<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.safesoftapplication">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:name=".SafeApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="Gestion comerciale"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.SafeSoftApplication">
        <activity
            android:name=".AccueilActivity"
            android:label="@string/title_activity_accueil"
            android:theme="@style/Theme.SafeSoftApplication.NoActionBar" />
        <activity android:name=".ui.inscription.InsriptionActivity" />
        <activity android:name=".ui.authentification.LoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>